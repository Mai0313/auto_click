{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Auto Click","text":""},{"location":"#description","title":"Description","text":"<p>This is a information grabber for pictures. It can be used to extract information from pictures and store them in a database.</p>"},{"location":"API/compare/","title":"Compare","text":""},{"location":"API/compare/#src.compare","title":"compare","text":""},{"location":"API/compare/#src.compare.ImageComparison","title":"ImageComparison","text":"<p>               Bases: <code>BaseModel</code></p> <p>Represents an image comparison object.</p> <p>This class provides methods to compare an image with a screenshot and find the position of a button image within the screenshot.</p> <p>Attributes:</p> Name Type Description <code>image_cfg</code> <code>ImageModel</code> <p>The image configuration.</p> <code>screenshot</code> <code>Union[Image, bytes]</code> <p>The screenshot image.</p> <code>device</code> <code>Union[Page, AdbDevice, ShiftPosition]</code> <p>The device.</p> <p>Methods:</p> Name Description <code>__screenshot_array</code> <p>Converts the screenshot to color and grayscale arrays.</p> <code>__draw_rectangle</code> <p>Draws a rectangle on the image and saves the resulting image.</p> <code>find</code> <p>Finds the position of a button image within a screenshot.</p>"},{"location":"API/compare/#src.compare.ImageComparison.find","title":"find","text":"<pre><code>find() -&gt; FoundPosition\n</code></pre> <p>Finds the position of a button image within a screenshot.</p> <p>Returns:</p> Name Type Description <code>button_center_x</code> <code>int</code> <p>The x-coordinate of the button center.</p> <code>button_center_y</code> <code>int</code> <p>The y-coordinate of the button center.</p> <code>color_screenshot</code> <code>ndarray</code> <p>The screenshot with the red rectangle drawn on it.</p> Todo <p>Add logging functionality</p> Source code in <code>src/compare.py</code> <pre><code>def find(self) -&gt; FoundPosition:\n    \"\"\"Finds the position of a button image within a screenshot.\n\n    Returns:\n        button_center_x (int): The x-coordinate of the button center.\n        button_center_y (int): The y-coordinate of the button center.\n        color_screenshot (np.ndarray): The screenshot with the red rectangle drawn on it.\n\n    Todo:\n        Add logging functionality\n    \"\"\"\n    button_center_x, button_center_y = 0, 0\n    _, gray_screenshot = self.__screenshot_array\n    button_image = cv2.imread(self.image_cfg.image_path, 0)\n\n    result = cv2.matchTemplate(gray_screenshot, button_image, cv2.TM_CCOEFF_NORMED)\n    _, max_val, _, max_loc = cv2.minMaxLoc(result)\n\n    button_center_x = int(max_loc[0] + button_image.shape[1])\n    button_center_y = int(max_loc[1] + button_image.shape[0])\n    matched_image_position = (button_center_x, button_center_y)\n\n    color_screenshot = self.__draw_rectangle(\n        matched_image_position=matched_image_position, max_loc=max_loc, draw_black=False\n    )\n    blackout_screenshot = self.__draw_rectangle(\n        matched_image_position=matched_image_position, max_loc=max_loc, draw_black=True\n    )\n\n    if max_val &gt; self.image_cfg.confidence:\n        return FoundPosition(\n            button_center_x=button_center_x,\n            button_center_y=button_center_y,\n            color_screenshot=color_screenshot,\n            blackout_screenshot=blackout_screenshot,\n        )\n    return FoundPosition(\n        button_center_x=None,\n        button_center_y=None,\n        color_screenshot=color_screenshot,\n        blackout_screenshot=blackout_screenshot,\n    )\n</code></pre>"},{"location":"API/screenshot/","title":"Screenshot","text":""},{"location":"API/screenshot/#src.get_screen","title":"get_screen","text":""},{"location":"API/screenshot/#src.get_screen.GetScreen","title":"GetScreen","text":"<p>               Bases: <code>BaseModel</code></p> <p>A class for capturing screenshots from different sources.</p> <p>Attributes:</p> Name Type Description <code>screenshot</code> <code>Screenshot</code> <p>The screenshot object.</p> <p>Methods:</p> Name Description <code>from_exist_window</code> <p>Capture a screenshot from an existing window.</p> <code>from_adb_device</code> <p>Create a screenshot from an Android device using ADB.</p> <code>from_remote_window</code> <p>Create a screenshot of a remote window using Playwright.</p>"},{"location":"API/screenshot/#src.get_screen.GetScreen.from_exist_window","title":"from_exist_window","text":"<pre><code>from_exist_window(window_title: str) -&gt; Screenshot\n</code></pre> <p>Capture a screenshot from an existing window.</p> <p>Parameters:</p> Name Type Description Default <code>window_title</code> <code>str</code> required <p>Parameters:</p> Name Type Description Default <code>window_title</code> <code>str</code> <p>The title of the window to capture.</p> required <p>Returns:</p> Name Type Description <code>Screenshot</code> <code>Screenshot</code> <p>An object containing the captured screenshot and the shift position.</p> Source code in <code>src/get_screen.py</code> <pre><code>@classmethod\ndef from_exist_window(cls, window_title: str) -&gt; Screenshot:\n    \"\"\"Capture a screenshot from an existing window.\n\n    Args:\n        window_title (str): The title of the window to capture.\n\n    Returns:\n        Screenshot: An object containing the captured screenshot and the shift position.\n\n    \"\"\"\n    window = gw.getWindowsWithTitle(window_title)[0]  # type: Win32Window\n    if window.isMinimized:\n        window.restore()\n    window.activate()\n    time.sleep(0.1)\n    shift_x, shift_y = window.topleft\n    width, height = window.size\n    bbox = (shift_x, shift_y, shift_x + width, shift_y + height)\n    screenshot = ImageGrab.grab(bbox=bbox)\n    # For this method, there is always a shift position\n    shift_position = ShiftPosition(shift_x=shift_x, shift_y=shift_y)\n    return Screenshot(screenshot=screenshot, device=shift_position)\n</code></pre>"},{"location":"API/screenshot/#src.get_screen.GetScreen.from_adb_device","title":"from_adb_device","text":"<pre><code>from_adb_device(url: str, serial: str) -&gt; Screenshot\n</code></pre> <p>Create a Screenshot object from an Android device using ADB.</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> required <code>serial</code> <code>str</code> required <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>The URL of the app to be opened on the device.</p> required <code>serial</code> <code>str</code> <p>The serial number of the Android device.</p> required <p>Returns:</p> Name Type Description <code>Screenshot</code> <code>Screenshot</code> <p>An object containing the screenshot and device information.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>If the current app on the device is not the specified URL.</p> Source code in <code>src/get_screen.py</code> <pre><code>@classmethod\ndef from_adb_device(cls, url: str, serial: str) -&gt; Screenshot:\n    \"\"\"Create a Screenshot object from an Android device using ADB.\n\n    Args:\n        url (str): The URL of the app to be opened on the device.\n        serial (str): The serial number of the Android device.\n\n    Returns:\n        Screenshot: An object containing the screenshot and device information.\n\n    Raises:\n        Exception: If the current app on the device is not the specified URL.\n\n    \"\"\"\n    # os.system(f\".\\\\binaries\\\\adb.exe connect {serial}\")\n    # commands = [\"./binaries/adb.exe\", \"connect\", serial]\n    # command_executor = CommandExecutor(commands=commands)\n    # command_executor.run()\n    adb.connect(serial)\n    device = adb.device(serial=serial)\n    running_app = device.app_current()\n    if running_app.package != url:\n        # TODO: add log here.\n        raise Exception(\"The current app is not the specified URL\")\n    screenshot = device.screenshot()\n    return Screenshot(screenshot=screenshot, device=device)\n</code></pre>"},{"location":"API/screenshot/#src.get_screen.GetScreen.from_remote_window","title":"from_remote_window","text":"<pre><code>from_remote_window(url: str) -&gt; Screenshot\n</code></pre> <p>Create a screenshot of a remote window using Playwright.</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> required <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>The URL of the remote window.</p> required <p>Returns:</p> Name Type Description <code>Screenshot</code> <code>Screenshot</code> <p>An object containing the screenshot and the page.</p> Source code in <code>src/get_screen.py</code> <pre><code>@classmethod\ndef from_remote_window(cls, url: str) -&gt; Screenshot:\n    \"\"\"Create a screenshot of a remote window using Playwright.\n\n    Args:\n        url (str): The URL of the remote window.\n\n    Returns:\n        Screenshot: An object containing the screenshot and the page.\n\n    \"\"\"\n    with sync_playwright() as p:\n        browser = p.chromium.launch(headless=False)\n        context = browser.new_context(\n            user_agent=\"Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/58.0.3029.110 Safari/537.3\",\n            java_script_enabled=True,\n            accept_downloads=False,\n            has_touch=False,\n            is_mobile=False,\n            locale=\"zh-TW\",\n            permissions=[],\n            geolocation=None,\n            color_scheme=\"light\",\n            timezone_id=\"Asia/Shanghai\",\n        )\n        page = context.new_page()\n        page.goto(url)\n        screenshot = page.screenshot()\n        return Screenshot(screenshot=screenshot, device=page)\n</code></pre>"},{"location":"API/types/config/","title":"Config","text":""},{"location":"API/types/config/#src.types.config","title":"config","text":""},{"location":"API/types/config/#src.types.config.ConfigModel","title":"ConfigModel","text":"<p>               Bases: <code>BaseModel</code></p> <p>Represents the configuration model for auto click functionality.</p> <p>Attributes:</p> Name Type Description <code>target</code> <code>str</code> <p>The target of the auto click, which can be either a window title, a URL, or a CDP URL.</p> <code>auto_click</code> <code>bool</code> <p>Indicates whether auto click is enabled or not.</p> <code>random_interval</code> <code>int</code> <p>The random interval between auto clicks.</p> <code>image_list</code> <code>list[ImageModel]</code> <p>The list of image models to be used for auto click matching.</p>"},{"location":"API/types/image_models/","title":"Image models","text":""},{"location":"API/types/image_models/#src.types.image_models","title":"image_models","text":""},{"location":"API/types/image_models/#src.types.image_models.ImageModel","title":"ImageModel","text":"<p>               Bases: <code>BaseModel</code></p> <p>Represents an image model.</p> <p>Attributes:</p> Name Type Description <code>image_name</code> <code>str</code> <p>The name of the image.</p> <code>image_path</code> <code>str</code> <p>The path to the image.</p> <code>delay_after_click</code> <code>int</code> <p>The delay after the click when the image is found.</p> <code>click_this</code> <code>bool</code> <p>Indicates whether to click the image or not.</p> <code>screenshot_option</code> <code>bool</code> <p>Indicates whether to take a screenshot or not.</p> <code>confidence</code> <code>float</code> <p>The confidence level for image matching.</p>"},{"location":"API/types/output_models/","title":"Output models","text":""},{"location":"API/types/output_models/#src.types.output_models","title":"output_models","text":""},{"location":"API/types/output_models/#src.types.output_models.ShiftPosition","title":"ShiftPosition","text":"<p>               Bases: <code>BaseModel</code></p> <p>Represents a shift in position.</p> <p>Attributes:</p> Name Type Description <code>shift_x</code> <code>int</code> <p>The amount to shift in the x-axis.</p> <code>shift_y</code> <code>int</code> <p>The amount to shift in the y-axis.</p>"},{"location":"API/types/output_models/#src.types.output_models.FoundPosition","title":"FoundPosition","text":"<p>               Bases: <code>BaseModel</code></p> <p>Represents the position of a found button on the screen.</p> <p>Attributes:</p> Name Type Description <code>button_center_x</code> <code>Optional[int]</code> <p>The x-coordinate of the button center.</p> <code>button_center_y</code> <code>Optional[int]</code> <p>The y-coordinate of the button center.</p> <code>color_screenshot</code> <code>MatLike</code> <p>The screenshot of the button in color.</p> <code>blackout_screenshot</code> <code>MatLike</code> <p>The screenshot of the button with blackout effect.</p>"},{"location":"API/types/output_models/#src.types.output_models.Screenshot","title":"Screenshot","text":"<p>               Bases: <code>BaseModel</code></p> <p>Represents a screenshot captured from a device.</p> <p>Attributes:</p> Name Type Description <code>model_config</code> <code>ConfigDict</code> <p>The configuration dictionary for the model.</p> <code>screenshot</code> <code>Union[bytes, Image]</code> <p>The screenshot image data.</p> <code>device</code> <code>Union[AdbDevice, Page, ShiftPosition]</code> <p>The device from which the screenshot was captured.</p> <p>Methods:</p> Name Description <code>save</code> <p>Save the screenshot to a specified path.</p> <code>calibrate</code> <p>Adjust the button center coordinates based on the device</p>"},{"location":"API/types/output_models/#src.types.output_models.Screenshot.save","title":"save","text":"<pre><code>save(save_path: str) -&gt; str\n</code></pre> <p>Save the screenshot to the specified path.</p> <p>Parameters:</p> Name Type Description Default <code>save_path</code> <code>str</code> required <p>Parameters:</p> Name Type Description Default <code>save_path</code> <code>str</code> <p>The path where the screenshot should be saved.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The path where the screenshot was saved.</p> Source code in <code>src/types/output_models.py</code> <pre><code>def save(self, save_path: str) -&gt; str:\n    \"\"\"Save the screenshot to the specified path.\n\n    Args:\n        save_path (str): The path where the screenshot should be saved.\n\n    Returns:\n        str: The path where the screenshot was saved.\n    \"\"\"\n    save_path = f\"{save_path}.png\" if not save_path.endswith(\".png\") else save_path\n    if isinstance(self.screenshot, bytes):\n        with open(save_path, \"wb\") as f:\n            f.write(self.screenshot)\n    elif isinstance(self.screenshot, Image.Image):\n        self.screenshot.save(save_path)\n    return save_path\n</code></pre>"},{"location":"API/types/output_models/#src.types.output_models.Screenshot.calibrate","title":"calibrate","text":"<pre><code>calibrate(button_center_x: int, button_center_y: int) -&gt; tuple[int, int]\n</code></pre> <p>Adjusts the button center coordinates based on the device type.</p> <p>Parameters:</p> Name Type Description Default <code>button_center_x</code> <code>int</code> required <code>button_center_y</code> <code>int</code> required <p>Parameters:</p> Name Type Description Default <code>button_center_x</code> <code>int</code> <p>The x-coordinate of the button center.</p> required <code>button_center_y</code> <code>int</code> <p>The y-coordinate of the button center.</p> required <p>Returns:</p> Type Description <code>tuple[int, int]</code> <p>tuple[int, int]: The adjusted button center coordinates.</p> Source code in <code>src/types/output_models.py</code> <pre><code>def calibrate(self, button_center_x: int, button_center_y: int) -&gt; tuple[int, int]:\n    \"\"\"Adjusts the button center coordinates based on the device type.\n\n    Args:\n        button_center_x (int): The x-coordinate of the button center.\n        button_center_y (int): The y-coordinate of the button center.\n\n    Returns:\n        tuple[int, int]: The adjusted button center coordinates.\n    \"\"\"\n    # if the device is from a window process, we need to add shift_x, shift_y to the button_center_x, button_center_y\n    # since we do not know the exact position of the window.\n    if isinstance(self.device, Page):\n        # button_center_x = button_center_x // 2\n        # button_center_y = button_center_y // 2\n        pass\n    if isinstance(self.device, AdbDevice):\n        # button_center_x = button_center_x // 2\n        # button_center_y = button_center_y // 2\n        pass\n    if isinstance(self.device, ShiftPosition):\n        button_center_x = button_center_x + self.device.shift_x\n        button_center_y = button_center_y + self.device.shift_y\n    return button_center_x, button_center_y\n</code></pre>"},{"location":"API/utils/adb/","title":"Adb","text":""},{"location":"API/utils/adb/#src.utils.get_serial","title":"get_serial","text":""},{"location":"API/utils/adb/#src.utils.get_serial.ADBDeviceManager","title":"ADBDeviceManager","text":"<p>               Bases: <code>BaseModel</code></p> <p>Class for managing ADB devices and retrieving device information.</p> <p>Attributes:</p> Name Type Description <code>host</code> <code>str</code> <p>The host IP address.</p> <code>target</code> <code>str</code> <p>The target app package name.</p> <p>Methods:</p> Name Description <code>available_devices</code> <p>Returns a list of available devices.</p> <code>serials</code> <p>Retrieves a list of serial numbers for connected devices.</p> <code>running_apps</code> <p>Retrieves the running apps on the connected devices.</p> <code>get_correct_serial</code> <p>Returns the correct serial number for the target app.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>If multiple or no matching apps are found.</p>"},{"location":"API/utils/adb/#src.utils.get_serial.ADBDeviceManager.available_devices","title":"available_devices","text":"<pre><code>available_devices: list[int]\n</code></pre> <p>Returns a list of available devices.</p> <p>Returns:</p> Type Description <code>list[int]</code> <p>list[int]: A list of available devices.</p>"},{"location":"API/utils/adb/#src.utils.get_serial.ADBDeviceManager.serials","title":"serials","text":"<pre><code>serials: list[str]\n</code></pre> <p>Retrieves a list of serial numbers for connected devices.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>A list of serial numbers for connected devices.</p>"},{"location":"API/utils/adb/#src.utils.get_serial.ADBDeviceManager.running_apps","title":"running_apps","text":"<pre><code>running_apps: list[AppInfo]\n</code></pre> <p>Retrieves the running apps on the connected devices.</p> <p>Returns:</p> Type Description <code>list[AppInfo]</code> <p>A list of AppInfo objects representing the running apps on the connected devices.</p>"},{"location":"API/utils/adb/#src.utils.get_serial.ADBDeviceManager.get_correct_serial","title":"get_correct_serial","text":"<pre><code>get_correct_serial() -&gt; AppInfo\n</code></pre> <p>Returns the correct serial number for the target app.</p> <p>This method searches for the running apps that match the target app's package name. If there are multiple or no matching apps found, an exception is raised. Otherwise, the serial number of the first matching app is returned.</p> <p>Returns:</p> Name Type Description <code>AppInfo</code> <code>AppInfo</code> <p>The app information for the correct serial number.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>If multiple or no matching apps are found.</p> Source code in <code>src/utils/get_serial.py</code> <pre><code>def get_correct_serial(self) -&gt; AppInfo:\n    \"\"\"Returns the correct serial number for the target app.\n\n    This method searches for the running apps that match the target app's package name.\n    If there are multiple or no matching apps found, an exception is raised.\n    Otherwise, the serial number of the first matching app is returned.\n\n    Returns:\n        AppInfo: The app information for the correct serial number.\n\n    Raises:\n        Exception: If multiple or no matching apps are found.\n    \"\"\"\n    apps = [r_app for r_app in self.running_apps if r_app.package == self.target]\n    if len(apps) &gt; 1 or len(apps) == 0:\n        raise Exception(\"Multiple or no devices found\")\n    app = next(iter(apps))\n    return app\n</code></pre>"},{"location":"API/utils/command/","title":"Command","text":""},{"location":"API/utils/command/#src.utils.command_utils","title":"command_utils","text":""},{"location":"API/utils/command/#src.utils.command_utils.CommandExecutor","title":"CommandExecutor","text":"<p>               Bases: <code>BaseModel</code></p> <p>A class for executing shell commands.</p> <p>Attributes:</p> Name Type Description <code>commands</code> <code>list[str]</code> <p>A list of commands to be executed.</p> <p>Methods:</p> Name Description <code>run</code> <p>Executes the command and captures the output.</p> <code>popen</code> <p>Executes the command using subprocess.Popen and returns the stdout and stderr outputs</p>"},{"location":"API/utils/command/#src.utils.command_utils.CommandExecutor.run","title":"run","text":"<pre><code>run() -&gt; tuple[str, str]\n</code></pre> <p>Executes the command and captures the output.</p> <p>Returns:</p> Type Description <code>tuple[str, str]</code> <p>A tuple containing the stdout and stderr as strings.</p> Source code in <code>src/utils/command_utils.py</code> <pre><code>def run(self) -&gt; tuple[str, str]:\n    \"\"\"Executes the command and captures the output.\n\n    Returns:\n        A tuple containing the stdout and stderr as strings.\n    \"\"\"\n    connection_result = subprocess.run(self.commands, capture_output=True, text=True)  # noqa: S603\n    stdout = connection_result.stdout\n    stderr = connection_result.stderr\n    return stdout, stderr\n</code></pre>"},{"location":"API/utils/command/#src.utils.command_utils.CommandExecutor.popen","title":"popen","text":"<pre><code>popen() -&gt; tuple[str, str]\n</code></pre> <p>Executes the command using subprocess.Popen and returns the stdout and stderr outputs.</p> <p>Returns:</p> Type Description <code>tuple[str, str]</code> <p>A tuple containing the stdout and stderr outputs as strings.</p> Source code in <code>src/utils/command_utils.py</code> <pre><code>def popen(self) -&gt; tuple[str, str]:\n    \"\"\"Executes the command using subprocess.Popen and returns the stdout and stderr outputs.\n\n    Returns:\n        A tuple containing the stdout and stderr outputs as strings.\n    \"\"\"\n    connection_result_popen = subprocess.Popen(  # noqa: S603\n        self.commands, stdout=subprocess.PIPE, stderr=subprocess.PIPE, text=True\n    )\n    stdout, stderr = connection_result_popen.communicate()\n    return stdout, stderr\n</code></pre>"},{"location":"blog/","title":"Blog","text":""}]}